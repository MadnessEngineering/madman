name: Sync SKILL.md and rules

on:
  push:
    branches: [main]
    paths:
      - skills/mad-tinker/SKILL.md
      - rules/mad-tinker-activate.md
      - tinker-compress/SKILL.md
      - tinker-compress/scripts/**

concurrency:
  group: sync-skill
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Pull latest before making changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Sync SKILL.md copies
        run: |
          cp skills/mad-tinker/SKILL.md mad-tinker/SKILL.md
          cp skills/mad-tinker/SKILL.md plugins/mad-tinker/skills/mad-tinker/SKILL.md
          cp skills/mad-tinker/SKILL.md .cursor/skills/mad-tinker/SKILL.md
          mkdir -p .windsurf/skills/tinker
          cp skills/mad-tinker/SKILL.md .windsurf/skills/mad-tinker/SKILL.md

      - name: Sync compress skill
        run: |
          # Sync to skills/compress (main location for Gemini CLI)
          rm -rf skills/compress/scripts
          cp tinker-compress/SKILL.md skills/compress/SKILL.md
          sed -i 's/^name: tinker-compress$/name: compress/' skills/compress/SKILL.md
          sed -i "s|The compression scripts live in \`tinker-compress/scripts/\` (adjacent to this SKILL.md). If the path is not immediately available, search for \`tinker-compress/scripts/__main__.py\`.|This SKILL.md lives alongside \`scripts/\` in the same directory. Find that directory.|" skills/compress/SKILL.md
          sed -i 's|cd tinker-compress && python3|cd <directory_containing_this_SKILL.md> \&\& python3|' skills/compress/SKILL.md
          cp -r tinker-compress/scripts skills/compress/scripts
          rm -rf skills/compress/scripts/__pycache__

      - name: Sync compress skill to plugin
        run: |
          # Copy SKILL.md and patch name + process instructions for plugin context
          cp tinker-compress/SKILL.md plugins/mad-tinker/skills/compress/SKILL.md
          sed -i 's/^name: tinker-compress$/name: compress/' plugins/mad-tinker/skills/compress/SKILL.md
          sed -i "s|The compression scripts live in \`tinker-compress/scripts/\` (adjacent to this SKILL.md). If the path is not immediately available, search for \`tinker-compress/scripts/__main__.py\`.|This SKILL.md lives alongside \`scripts/\` in the same directory. Find that directory.|" plugins/mad-tinker/skills/compress/SKILL.md
          sed -i 's|cd tinker-compress && python3|cd <directory_containing_this_SKILL.md> \&\& python3|' plugins/mad-tinker/skills/compress/SKILL.md
          # Copy scripts verbatim
          rm -rf plugins/mad-tinker/skills/compress/scripts
          cp -r tinker-compress/scripts plugins/mad-tinker/skills/compress/scripts
          rm -rf plugins/mad-tinker/skills/compress/scripts/__pycache__

      - name: Rebuild mad-tinker.skill ZIP
        run: cd skills && zip -r ../mad-tinker.skill tinker/

      - name: Sync auto-activation rules
        run: |
          BODY="rules/mad-tinker-activate.md"

          # Cline + Copilot — no frontmatter, direct copy
          mkdir -p .clinerules .github
          cp "$BODY" .clinerules/mad-tinker.md
          cp "$BODY" .github/copilot-instructions.md

          # Cursor — needs alwaysApply frontmatter
          mkdir -p .cursor/rules
          printf '%s\n' '---' 'description: "Mad Tinker mode — terse communication, ~75% fewer tokens, full technical accuracy"' 'alwaysApply: true' '---' '' > .cursor/rules/mad-tinker.mdc
          cat "$BODY" >> .cursor/rules/mad-tinker.mdc

          # Windsurf — needs always_on frontmatter
          mkdir -p .windsurf/rules
          printf '%s\n' '---' 'trigger: always_on' '---' '' > .windsurf/rules/mad-tinker.md
          cat "$BODY" >> .windsurf/rules/mad-tinker.md

      - name: Commit and push if changed
        run: |
          git add \
            mad-tinker/SKILL.md \
            skills/compress/ \
            plugins/mad-tinker/skills/compress/ \
            plugins/mad-tinker/skills/mad-tinker/SKILL.md \
            .cursor/skills/mad-tinker/SKILL.md \
            .windsurf/skills/mad-tinker/SKILL.md \
            mad-tinker.skill \
            .clinerules/mad-tinker.md \
            .github/copilot-instructions.md \
            .cursor/rules/mad-tinker.mdc \
            .windsurf/rules/mad-tinker.md
          git diff --staged --quiet && exit 0
          git commit -m "chore: sync SKILL.md copies and auto-activation rules [skip ci]"
          git push
